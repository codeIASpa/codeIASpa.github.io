---
import { Picture } from "astro:assets";
import heroImage from "assets/codeia_1.png";
import Link from "@components/ui/link.astro";
import { Icon } from "astro-icon/components";
---

<main
  class="grid lg:grid-cols-2 place-items-center pt-16 pb-8 md:pt-12 md:pb-24">
  <div class="py-6 md:order-1 hidden md:block">
    <Picture
      id="astronaut"
      src={heroImage}
      alt="Astronaut in the air"
      widths={[200, 400, 600]}
      sizes="(max-width: 800px) 100vw, 620px"
      loading="eager"
      format="avif"
      class="h-full w-full object-cover"
    />
  </div>
  <div>
    <h1
      class="text-5xl lg:text-6xl xl:text-7xl font-bold lg:tracking-tight xl:tracking-tighter dark:text-violet-300">
      CodeIA: Innovación en Inteligencia Artificial y Desarrollo de Software
    </h1>
    <p class="text-lg mt-4 text-slate-600 max-w-xl dark:text-slate-100">
      Ayudamos a empresas en Chile y América Latina a resolver problemas complejos con soluciones tecnológicas personalizadas. <br />
      <span class="text-sm mt-2 text-slate-600 max-w-xl dark:text-pink-400">
        Combinamos IA, desarrollo web y software inteligente para impulsar cambios significativos en la región.
      </span>
    </p>
    <div class="mt-6 flex flex-col sm:flex-row gap-3">
      <Link
        href="https://api.whatsapp.com/send?phone=56936418650"
        target="_blank"
        class="flex gap-1 items-center justify-center"
        rel="noopener">
        <Icon class="w-5 h-5 text-[#25D366]" name="bx:bxl-whatsapp" />
        Contáctanos por WhatsApp
      </Link>
      <Link
        size="lg"
        style="outline"
        rel="noopener"
        href="https://github.com/codeIASpa"
        class="flex gap-1 items-center justify-center"
        target="_blank">
        <Icon class="text-black w-4 h-4" name="bx:bxl-github" />
        Nuestro GitHub
      </Link>
    </div>
  </div>
  
</main>

<style>
  @keyframes float {
    0% {
      transform: translate3d(0, 0, 0);
    }
    100% {
      transform: translate3d(0, 90px, 0);
    }
  }

  #astronaut {
    animation: float linear 5s infinite alternate;
  }

  @media (prefers-reduced-motion: reduce) {
    #astronaut {
      animation: none;
    }
  }
</style>
